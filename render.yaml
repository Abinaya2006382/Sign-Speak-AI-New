services:
  - type: web
    name: sign-speak-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "backend.app:create_app()"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9